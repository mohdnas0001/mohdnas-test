export enum Role {
  Mentor = 'mentor',
  Student = 'student',
}

export enum ProgramStatus {
  Active = 'active',
  Inactive = 'inactive',
}

export enum GroupCallStatus {
  Ongoing = 'ongoing',
  Upcoming = 'upcoming',
}
